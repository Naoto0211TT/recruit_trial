---
import ProductCard from '@/components/ui/ProductCard.astro';
import SectionHeading from '@/components/ui/SectionHeading.astro';
import { lpAssets } from '@/constants/assets';
import { product } from '@/constants/content';

const productImages = [
  lpAssets.product01,
  lpAssets.product02,
  lpAssets.product03,
  lpAssets.product04,
];
---

<section
  class="bg-white"
  id="product"
>
  <div class="bg-primary px-[2rem] pt-[6.4rem] pb-[13.2rem] md:px-0 md:pt-[136px] md:pb-[272px]">
    <div
      class="flex flex-col gap-[3.2rem] md:mx-auto md:max-w-[1440px] md:gap-[40px] md:px-[120px]"
    >
      <SectionHeading
        color="white"
        en={product.heading.en}
        sub={product.heading.sub}
      />
      <div class="flex flex-col gap-[2rem] text-white md:gap-[24px]">
        <h2
          class="leading-heading text-[2.1rem] font-medium tracking-[0.525px] md:text-[24px] md:tracking-[0.6px]"
        >
          {product.lead}
        </h2>
        <p class="leading-body text-[1.6rem] tracking-[0.4px]">{product.body}</p>
      </div>
    </div>
  </div>

  <div
    class="relative -mt-[9.2rem] grid grid-cols-1 gap-[4rem] px-[2rem] pb-[6.4rem] md:mx-auto md:-mt-[195px] md:max-w-[1440px] md:grid-cols-2 md:gap-x-[60px] md:gap-y-[60px] md:px-[120px] md:pb-[144px]"
  >
    {
      product.items.map((item, index) => (
        <ProductCard
          description={product.cardBody}
          image={productImages[index] ?? lpAssets.product01}
          imageAlt={`${item.title}のイメージ`}
          title={item.title}
        />
      ))
    }
  </div>
</section>
